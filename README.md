# 图片压缩工具

这是一个简单的图片压缩工具，允许用户上传图片并设置压缩质量。

## 功能特性
- 支持图片上传
- 可调整压缩质量
- 实时预览压缩后的图片
- 保存压缩后的图片到本地

## 使用方法
1. 打开 `index.html` 文件。
2. 点击 `将图片拖放到此处或点击选择文件` 选择要压缩的图片。
3. 通过滑动条调整压缩质量。
4. 点击 `确认` 进行确认操作。
5. 点击 `保存到本地` 保存压缩后的图片。

## 技术栈
- HTML
- CSS
- JavaScript

## 依赖
无